{
  "name": "@good-first-issue/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "good-first-issue": "./bin/good-first-issue.js"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "pnpm build && ./bin/good-first-issue.js",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@good-first-issue/core": "workspace:*",
    "@good-first-issue/types": "workspace:*"
  }
}
